# nothing

This is just the `archlinux` image, updated more frequently. Use this if you just need arch and _nothing_ else.

In future, I might make the builder image manually build what's in the `archlinux` image.
