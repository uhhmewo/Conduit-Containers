#!/usr/bin/env bash
set -eax

export NAME="$(jq -r .name package.json)"
export AUTHOR="$(jq -r .author package.json)"
export IMAGE="$AUTHOR/$NAME:${DOCKER_TAG:-latest}"
export OCI_HOST="${OCI_HOST:-docker}"
$OCI_HOST build $@ -t $IMAGE .
