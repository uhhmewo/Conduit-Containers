{
  "name": "containers",
  "version": "1.0.0-dev",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many --target=build",
    "build:no-ci": "nx run-many --target=build-no-ci",
    "pub": "nx run-many --target=publish-no-ci"
  },
  "private": true,
  "devDependencies": {
    "@nx/js": "18.2.3",
    "changeset": "^0.2.6",
    "nx": "18.2.3"
  }
}
