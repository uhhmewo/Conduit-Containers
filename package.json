{
  "name": "containers",
  "version": "1.0.0-dev",
  "license": "MIT",
  "scripts": {
    "build": "nx run-many --target=build --parallel=16 --nxBail",
    "pub": "nx run-many --target=publish-no-ci"
  },
  "private": true,
  "devDependencies": {
    "@nx/js": "19.3.0",
    "changeset": "^0.2.6",
    "nx": "19.3.0"
  }
}
